# age_regression_progression
This repository implements a deep learning–based facial age regression and progression pipeline built on top of a pretrained StyleGAN2 generator. The system encodes a real face image into latent space using a SAM (Style-Aware Mapper) encoder and applies a lightweight latent adapter to model age-related transformations while preserving identity.
